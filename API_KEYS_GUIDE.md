# Solar API 키 설정 가이드

## 📝 개요

22명의 학생을 위해 6개의 Solar API 키를 사용합니다 (학생 4명당 1개의 키).

## 🔑 API 키 발급 방법

1. **Upstage Console 접속**

   - URL: https://console.upstage.ai/
   - 회원가입 또는 로그인

2. **API Keys 메뉴 이동**

   - 좌측 메뉴에서 "API Keys" 클릭

3. **6개의 키 생성**

   - "Create API Key" 버튼 클릭
   - 총 6번 반복하여 6개의 키 생성
   - 각 키에 이름을 지정할 수 있습니다 (예: "프롬프톤-키1", "프롬프톤-키2", ...)

4. **키 복사**
   - 생성된 키를 안전한 곳에 복사해두세요

## ⚙️ .env 파일 설정

프로젝트 루트 디렉토리의 `.env` 파일을 열어서 API 키를 입력하세요:

```bash
cd "/Users/downy/Documents/SW Supporters Prompthon"
nano .env  # 또는 원하는 에디터로 열기
```

`.env` 파일 내용:

```env
SOLAR_API_KEY_1=실제_발급받은_키_1
SOLAR_API_KEY_2=실제_발급받은_키_2
SOLAR_API_KEY_3=실제_발급받은_키_3
SOLAR_API_KEY_4=실제_발급받은_키_4
SOLAR_API_KEY_5=실제_발급받은_키_5
SOLAR_API_KEY_6=실제_발급받은_키_6
```

⚠️ **주의**: `your_api_key_X_here`를 실제 발급받은 키로 **반드시** 교체하세요!

## 🔄 작동 방식

### 라운드로빈 부하 분산

- 각 프롬프트 제출마다 순차적으로 다른 API 키 사용
- 6개의 키를 순환하며 사용하므로 부하가 고르게 분산됨
- 예:
  - 1번 학생 제출 → 키1 사용
  - 2번 학생 제출 → 키2 사용
  - ...
  - 7번 학생 제출 → 키1 사용 (순환)

### 동시 처리 최적화

- API 키 개수에 따라 동시 처리 수 자동 조정
- 6개 키 사용 시: 최대 30개 문장 동시 처리 (키당 5개)
- 더 빠른 처리 속도 제공

### 자동 감지

- 시스템이 자동으로 유효한 키 개수를 감지
- 1개만 설정해도 작동 (호환성)
- 6개 모두 설정하면 최고 성능

## ✅ 설정 확인

서버 시작 시 다음과 같은 메시지가 표시되어야 합니다:

```
✅ 6개의 Solar API 키를 로드했습니다.
```

또는 헬스 체크 엔드포인트로 확인:

```bash
curl http://localhost:8000/api/health
```

응답에서 `"solar_client_ready": true`가 표시되면 정상입니다.

## 🐛 문제 해결

### "Solar API 키가 설정되지 않았습니다" 오류

- `.env` 파일이 프로젝트 루트에 있는지 확인
- API 키가 `your_api_key`로 시작하지 않는지 확인
- 최소 1개 이상의 유효한 키가 있는지 확인

### 서버가 시작되지 않음

```bash
# 서버 재시작
pkill -f "uvicorn main:app"
cd "/Users/downy/Documents/SW Supporters Prompthon"
./run.sh
```

### API 키 개수 변경

- 6개보다 적게 사용해도 됨
- 더 많이 사용하려면 `solar_api.py`의 `range(1, 7)` 수정
- 예: 10개 사용 시 → `range(1, 11)`

## 💰 비용 관리

- Solar API는 사용량에 따라 과금됩니다
- 6개 키로 나누면 각 키의 사용량이 분산되어 관리가 용이합니다
- Upstage Console에서 각 키별 사용량 모니터링 가능

## 📊 권장 사항

### 대회 전

1. 모든 키의 잔액/할당량 확인
2. 테스트 제출로 정상 작동 확인
3. 각 키별 사용 한도 설정 (선택사항)

### 대회 중

1. Upstage Console에서 실시간 사용량 모니터링
2. 특정 키에 문제 발생 시 해당 키만 제거하고 계속 진행 가능

### 대회 후

1. 총 사용량 및 비용 확인
2. 키 비활성화 또는 삭제 (보안을 위해)
