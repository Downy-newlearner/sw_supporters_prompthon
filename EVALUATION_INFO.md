# 평가 시스템 안내

## 📊 평가 데이터

### 테스트 데이터 생성 방법
- **소스**: `train.csv`에서 1000개 샘플 추출 (random_state=42)
- **테스트 파일**: `test_from_train.csv` (오류 문장만 포함)
- **정답 파일**: `answer_from_train.csv` (교정된 문장만 포함)

### 데이터 구조
```
test_from_train.csv:
- id, source, date, gender, age, err_sentence

answer_from_train.csv:
- id, cor_sentence
```

## 🎯 평가 기준

### TP / (TP + FP + FM) 방식

**공식**: `Score = TP / (TP + FP + FM) × 100`

### 용어 정의

#### TP (True Positive) - 올바른 교정
- 오류를 정확한 위치에서 올바르게 교정한 경우
- 예시:
  ```
  오류: "그래프를 보구판단할때"
  정답: "그래프를 보고 판단할 때"
  예측: "그래프를 보고 판단할 때"
  → TP: "구" → "고", "판단할때" → "판단할 때"
  ```

#### FP (False Positive) - 잘못된 교정
- 오류가 없는 곳을 교정한 경우
- 오류를 잘못된 방식으로 교정한 경우
- 예시:
  ```
  오류: "그래프를 보구판단할때"
  정답: "그래프를 보고 판단할 때"
  예측: "그래프를 봐서 판단할 때"
  → FP: "보구" → "봐서" (잘못된 교정)
  ```

#### FM (False Missing) - 놓친 교정
- 교정이 필요한 오류를 놓친 경우
- 예시:
  ```
  오류: "그래프를 보구판단할때"
  정답: "그래프를 보고 판단할 때"
  예측: "그래프를 보고판단할때"
  → FM: "판단할때" (띄어쓰기 교정 누락)
  ```

## 🔍 평가 알고리즘

### 1. 편집 거리 계산
Python의 `difflib.SequenceMatcher`를 사용하여 문자열 간 차이를 분석합니다.

### 2. 위치 기반 매칭
- 원본(err_sentence)과 정답(cor_sentence)의 차이점 파악
- 원본과 예측(predicted)의 차이점 파악
- 두 차이점을 비교하여 TP, FP, FM 계산

### 3. 문자 단위 카운팅
각 편집 연산에 대해 변경된 문자 수만큼 카운트합니다.

## 📈 점수 해석

- **100점**: 모든 오류를 완벽하게 교정
- **80-99점**: 대부분의 오류를 정확하게 교정
- **60-79점**: 많은 오류를 교정했으나 일부 누락 또는 오교정
- **40-59점**: 일부 오류 교정, 많은 개선 필요
- **0-39점**: 대부분의 오류 미교정 또는 과도한 오교정

## 💡 점수 향상 팁

### 1. 정확성 우선
- 확실하지 않은 부분은 교정하지 않는 것이 나을 수 있습니다
- 불필요한 교정은 FP로 카운트되어 점수를 낮춥니다

### 2. 문맥 이해
- 단순 맞춤법뿐만 아니라 문맥에 맞는 교정이 필요합니다
- 예: "보구" → "보고" (올바름), "보구" → "봐서" (문맥에 맞지 않으면 오류)

### 3. 일관성 유지
- 띄어쓰기, 맞춤법 등 일관된 규칙 적용
- 한 문장 내에서도 일관성 있는 교정 필요

### 4. 과도한 교정 주의
- 이미 올바른 부분을 변경하지 않기
- FP가 늘어나면 점수가 크게 하락

## 🔧 기술적 세부사항

### 편집 연산 유형
- `replace`: 문자 치환
- `insert`: 문자 삽입
- `delete`: 문자 삭제
- `equal`: 변경 없음 (평가에서 제외)

### 카운팅 방식
```python
# 변경된 문자 수만큼 카운트
length = len(target_text) if target_text else 1

# 예시:
"구" → "고": 1 카운트
"판단할때" → "판단할 때": 5 카운트
```

## 📊 결과 해석

제출 후 다음 정보가 표시됩니다:
- **최종 점수**: TP / (TP + FP + FM) × 100
- **TP**: 올바른 교정 횟수
- **FP**: 잘못된 교정 횟수
- **FM**: 놓친 교정 횟수

이 정보를 활용하여 프롬프트를 개선하세요!

---

**참고**: 이 평가 방식은 실제 한국어 문법 교정의 정확성을 종합적으로 평가하기 위해 설계되었습니다.

