# Nixpacks 빌드 설정 (Railway에서 사용)
[phases.setup]
nixPkgs = ["python3", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd backend && pip install -r requirements.txt"
]

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"

